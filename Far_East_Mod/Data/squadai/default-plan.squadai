
-- command macros
import("squadai-util")

-- squadai plan file

------------------------------------------------------------------------------
-- plan 

plan = 
{
	-- phase1
	--[[{
		type = DATA_PHASE,
		name = "START PLAN: all move",
		
		--
		{
			apply_to = ET_All,
			actions = 
			{
				ACTION_MOVE_POSTURE( DT_MAX_SQUAD_RANGE, 0.95, 10.0, 60.0, "squad_formations/squad_ai.lua", PACE_VERYFAST, PACE_FAST, POSTURE_CROUCH, POSTURE_PRONE, 0.5 ), 
			}
		},
	},]]
}
